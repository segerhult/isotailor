# Nginx

## Files
- `web/nginx.conf`

## Notes
- Nginx is used as a reverse proxy and static host.
- Refer to `web/nginx.conf` for the Nginx configuration file.
- See the `docker-compose.yml` file for details on how Nginx is run within the Docker environment, including local development and CI.
